# MOOR Reduced Exit Policy
#
# Only allows common web ports. Much safer for exit operators.
# Significantly fewer abuse complaints than the default policy.
# Users can still browse the web, use email (IMAPS/SMTPS), SSH, etc.
#
# Copy these lines into your exit relay config file.

ExitPolicy accept *:20-21     # FTP
ExitPolicy accept *:22        # SSH
ExitPolicy accept *:43        # WHOIS
ExitPolicy accept *:53        # DNS
ExitPolicy accept *:80        # HTTP
ExitPolicy accept *:110       # POP3
ExitPolicy accept *:143       # IMAP
ExitPolicy accept *:220       # IMAP3
ExitPolicy accept *:443       # HTTPS
ExitPolicy accept *:465       # SMTPS
ExitPolicy accept *:587       # SMTP submission
ExitPolicy accept *:706       # SILC
ExitPolicy accept *:749       # Kerberos
ExitPolicy accept *:873       # rsync
ExitPolicy accept *:993       # IMAPS
ExitPolicy accept *:995       # POP3S
ExitPolicy accept *:1194      # OpenVPN
ExitPolicy accept *:1723      # PPTP
ExitPolicy accept *:3690      # SVN
ExitPolicy accept *:4321      # RWHOIS
ExitPolicy accept *:5222-5223 # XMPP
ExitPolicy accept *:5228      # Android push
ExitPolicy accept *:8080      # HTTP alt
ExitPolicy accept *:8443      # HTTPS alt
ExitPolicy accept *:9418      # Git
ExitPolicy reject *:*         # Reject everything else
